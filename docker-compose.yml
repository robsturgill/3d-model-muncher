services:
  3d-model-muncher:
    build: .
    container_name: 3d-model-muncher
    ports:
      - "3000:3001"
    volumes:
      - ./models:/app/models
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
