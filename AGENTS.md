# AGENTS.md

See [CLAUDE.md](./CLAUDE.md) for repository guidance — project overview, development
commands, architecture, data storage, metadata lifecycle, and testing conventions.

That file is the single source of truth for coding agents working in this repository,
regardless of which agent you are. It is kept here as a pointer rather than a copy so
the two cannot drift apart.
